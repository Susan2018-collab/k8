apiVersion: v1
kind: Service
metadata:
  name: notifier-service
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: notifier
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP
