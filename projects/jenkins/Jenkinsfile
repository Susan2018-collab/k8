pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'docker build -t your-dockerhub/monitor-app .'
            }
        }
        stage('Push') {
            steps {
                sh 'docker push your-dockerhub/monitor-app'
            }
        }
    }
}
