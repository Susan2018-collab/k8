pipeline {
    agent any

    stages {

        stage('Build Image') {
            steps {
                sh 'docker build -t monitoring-app:v1.${BUILD_NUMBER} ./monitoring-app'
            }
        }
